# 独立 Web 管理后台使用教程（v2.10.0）

「QQ群智能守护者」插件内置一个**独立 Web 管理后台**：在机器人服务器上跑一个独立 HTTP 服务，
浏览器直接访问即可查看广告检测数据并管理，**不依赖 AstrBot 管理面板**。

## 一、开启后台

1. 在插件设置（或 `_conf_schema.json`）中开启并配置：

   | 配置项 | 推荐值 | 说明 |
   | ------ | ------ | ---- |
   | `ad_backend_enabled` | `true` | 独立后台总开关（默认关闭） |
   | `ad_backend_port` | `8765` | 后台端口（1-65535），需在防火墙放行 |
   | `ad_backend_token` | 自己设一个随机串 | 访问令牌，留空则不鉴权（仅建议内网） |

2. 保存配置并**重载插件**（插件管理 → 重载）。
3. 查看 AstrBot 日志，出现类似：
   ```
   [GroupMgr] 独立管理后台已启动: http://0.0.0.0:8765
   ```
   说明后台已启动。

## 二、访问后台

### 1. 服务器本机访问

```
http://127.0.0.1:8765
```

### 2. 局域网/远程访问

把 `127.0.0.1` 换成服务器 IP：

```
http://你的服务器IP:8765
```

> 记得在防火墙/安全组放行 8765 端口（云服务器需在控制台安全组入方向放行 TCP 8765）。

### 3. 配置了 Token 时

浏览器访问时在地址后加 `?token=你设置的令牌`：

```
http://你的服务器IP:8765/?token=你的令牌
```

首次打开后令牌会保存在浏览器本地，后续刷新无需再加。API 请求也支持 `X-Token` 请求头。

## 三、后台各页面说明

| 标签页 | 功能 |
| ------ | ---- |
| **总览** | 今日广告拦截数、图片/视频广告数、累计审核记录、最近拦截列表、拦截用户排行 Top10（每 15 秒自动刷新） |
| **违规记录** | 全部审核记录，支持按操作关键词筛选（如 `撤回`/`禁言`/`踢`）、分页查看，图片证据可直接点击放大 |
| **广告黑名单** | 感知哈希广告样本列表（哈希/命中次数/收录时间），支持删除单个样本（广告确认后自动学习） |
| **视频指纹** | 广告确认视频的指纹缓存（首帧哈希+总帧数），支持一键清空 |
| **分级处置** | 每个群每个用户的广告违规累计次数（首次/最近时间），支持重置单个记录 |
| **配置状态** | 只读展示当前关键配置（视频检测、黑名单、分级处置、视觉 Provider 等） |

## 四、安全提示

- **务必设置 `ad_backend_token`**：后台包含用户 QQ、消息内容等敏感信息，且支持删除黑名单/清空指纹/重置记录等写操作，暴露公网且无令牌时风险很高。
- 仅开放给可信 IP：如无必要，建议后台只在内网/本机访问，或通过防火墙限制来源 IP。
- 数据只读来源：后台展示的是插件的 SQLite 审核日志与本地 JSON（`hash_blacklist.json` / `video_fingerprint_cache.json` / `ad_escalation.json`），不会上传到任何第三方。

## 五、常见问题

### Q1：日志显示「独立后台需要 Quart 框架」

AstrBot 4.x+ 已内置 Quart。若仍提示，检查 AstrBot 版本或重装 `pip install quart`。

### Q2：访问提示「token 无效」

确认访问地址带了正确的 `?token=xxx`，且与配置 `ad_backend_token` 完全一致（注意空格）。

### Q3：远程访问不通

1. 确认插件日志显示后台已启动；
2. 确认服务器防火墙放行端口（`ufw allow 8765` 或云安全组）；
3. 确认端口未被其他程序占用（可改 `ad_backend_port`）。

### Q4：改端口/令牌后不生效

修改配置后需要**重载插件**才会重新启动后台。

### Q5：想关闭后台

把 `ad_backend_enabled` 设为 `false` 并重载插件即可。
