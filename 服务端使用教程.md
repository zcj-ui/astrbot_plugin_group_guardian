# 服务端使用教程（v2.10.0）

本教程面向**在服务器上部署 AstrBot + 本插件**的完整流程，覆盖：环境准备、安装、智谱视觉模型配置、
核心功能开启、独立 Web 后台、验证与排障。

---

## 一、服务端环境要求

| 项目 | 要求 |
| ---- | ---- |
| 操作系统 | Linux（推荐）/ Windows |
| Python | 3.10 及以上 |
| AstrBot | >= 4.24.2 |
| 内存 | 建议 >= 2GB；2 核 2G 可跑（按需关闭不必要功能） |
| 磁盘 | >= 1GB（含词库与日志） |
| 依赖 | `opencv-python-headless`、`numpy`（二维码解码/视频抽帧），随插件 requirements 自动安装 |

> 本插件所有数据只保存在服务器本地（SQLite + JSON），不上传任何第三方。

---

## 二、安装 AstrBot

### 方式 A：Docker（推荐，最省心）

```bash
git clone https://github.com/AstrBotDevs/AstrBot.git
cd AstrBot
# 修改 compose.yml 暴露端口后：
docker compose up -d
```

### 方式 B：源码 + pip

```bash
git clone https://github.com/AstrBotDevs/AstrBot.git
cd AstrBot
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
python main.py
```

启动后浏览器访问 `http://服务器IP:6185`（AstrBot 管理面板，首次设置管理员密码）。

---

## 三、安装插件（zip 包）

1. 将 `astrbot_plugin_group_guardian-v2.10.0.zip` 上传到服务器；
2. AstrBot 管理面板 → **插件管理 → 上传/安装本地插件**，选择 zip；
3. 插件列表出现「QQ群智能守护者」并**启用**；
4. 进入插件设置，勾选「我已阅读并同意免责声明」（必须，否则不工作）。

---

## 四、把模型接入机器人（核心：智谱 GLM-4V-Flash，免费）

> **先理解**：模型不在您服务器上，而是智谱**云端**提供的，通过 API 调用。您要做的只是：① 拿一把智谱的“钥匙”（API Key）→ ② 在 AstrBot 里把钥匙和模型地址告诉机器人 → ③ 让插件指定用这个模型识别图片/视频。

### 第 1 步：去智谱开“钥匙”（API Key）

1. 浏览器打开 https://bigmodel.cn ，注册并登录（手机号）；
2. **必须完成实名认证**（个人中心 → 实名认证），否则模型调用会报错；
3. 进入 **API Keys** 页面：https://bigmodel.cn/usercenter/proj-mgmt/apikeys
4. 点「**创建 API Key**」，复制生成的 Key（格式 `{ID}.{SECRET}`，中间有英文点，**只显示一次**，马上保存）。

### 第 2 步：在 AstrBot 里添加“模型提供商”（把模型接入机器人）

1. 打开 AstrBot 管理面板（浏览器访问 `http://服务器IP:6185`）；
2. 左侧菜单找到 **「服务提供商」**（或叫「模型提供商 / Provider」），点进去；
3. 点右上角「**新增服务提供商 / 添加提供商**」；
4. **类型**一栏选择：**「智谱 Chat Completion 提供商适配器」**；如果列表里没有，就选 **「OpenAI」**（智谱兼容 OpenAI 接口）；
5. 填写三个关键字段：

   | 字段 | 填什么 | 说明 |
   | ---- | ------ | ---- |
   | **API Key** | 第 1 步复制的智谱 Key | 形如 `1a2b3c4d.xxxxxxxx` |
   | **API Base URL** | `https://open.bigmodel.cn/api/paas/v4/` | 智谱官方 OpenAI 兼容地址，别漏 `v4` |
   | **模型名称** | 点「获取模型列表」，选 `glm-4v-flash` | 免费视觉模型，别选错成 `glm-4v` |

6. 勾选该模型的「**启用**」开关；
7. 点「**测试**」→ 显示成功提示 → 点「保存」。
   > 如果测试报 401，是 Key 不对或没实名；报 404/模型不存在，是模型名写错了。

### 第 3 步：让插件使用这个模型（接入完成的关键一步）

插件默认不会自动用智谱模型，需要在**插件设置**里指定：

| 配置项 | 操作 |
| ------ | ---- |
| `ocr_provider_id` | 选成**第 2 步添加的那个智谱 Provider**（下拉框里选它的 ID） |
| `ocr_enabled` | 开启（图片识图审核） |
| `video_audit_enabled` | 开启（视频广告检测，和图片共用这个模型） |
| `qrcode_decode_enabled` | 开启（本地二维码解码，免费不耗模型） |

### 第 4 步：验证模型是否真正接入

1. 群内发一张带广告文字的图片 → 机器人应自动撤回并提示（说明模型识别成功）；
2. 发一个含二维码/广告画面的视频 → 应被撤回；
3. 若没反应，看 AstrBot 日志：
   - 搜 `OCR LLM调用失败` → 说明模型没调通，回第 2 步检查 Key/Base URL/模型名；
   - 搜 `[GroupMgr]` 能看到完整审核过程。
### 识别引擎总览（v2.11.0，默认不再用智谱）

`ocr_engine` 配置选择用哪个模型识别广告，默认 **`local`（本地）**：

| 值 | 说明 | 需要 |
| -- | ---- | ---- |
| `local`（默认） | 本地 RapidOCR | 开启后自动下载模型（约30MB） |
| `umi` | Umi-OCR（Rapid引擎版）本地HTTP服务 | 自行安装运行 Umi-OCR（默认 127.0.0.1:1224） |
| `cloud` | 第三方云广告检测API（如阿里云） | 配置 `cloud_audit_url`/`cloud_audit_api_key` |
| `llm` | 云端视觉模型（智谱等，可选） | 配置 `ocr_provider_id` |
| `auto` | 本地优先，识别不到回退云端 | 本地 + 可选云端 |

### 方案 A：本地 RapidOCR（默认，模型按需下载）

1. 插件设置把 `ocr_engine` 改为 `local`（默认即是）；
2. **首次启用会自动下载模型**：插件自动 `pip install rapidocr_onnxruntime`（含模型约 30MB）；
   - 若不想自动下载，把 `local_ocr_auto_install` 关掉，手动 `pip install rapidocr_onnxruntime`；
3. **关闭引擎不会卸载模型**：关闭 `local` 引擎后模型保留在服务器，下次开启直接可用。

> RapidOCR 常驻内存约 300MB，2 核 2G 可跑；二维码仍用本地 OpenCV 免费解码。

### 方案 B：Umi-OCR（Rapid 引擎版，可选附加项）

专门为图片/视频广告服务的本地 OCR 方案：

1. 下载并运行 **Umi-OCR**（Rapid 引擎版），开启「HTTP 服务」（默认端口 1224）；
2. 插件设置：`ocr_engine = umi`，`umi_ocr_url = http://127.0.0.1:1224`；
3. 图片/视频帧文字会通过 Umi-OCR 的 `/api/ocr` 接口识别，全程本地、不耗云端额度。

### 方案 C：第三方云广告检测 API（如阿里云内容安全）

1. 开通阿里云内容安全（或腾讯/百度等）图片广告检测，拿到 API 地址与 Key；
2. 插件设置：
   - `ocr_engine = cloud`
   - `cloud_audit_url` = 你的 API 地址
   - `cloud_audit_api_key` = 你的 Key
   - `cloud_audit_threshold` = 判定阈值（默认 0.8）
3. 图片/视频帧会 POST `{image_base64}` 到该地址，返回 `{is_ad, score, reason}`，命中即判定广告。

> 云 API 需自行适配其返回格式；识别出的广告标记同样进入统一审核流程。

### 智谱（llm）不再默认

v2.11.0 起默认用本地引擎；若仍需智谱视觉模型，把 `ocr_engine` 改为 `llm` 并配置 `ocr_provider_id`（详见上节）。
## 五、插件核心配置（逐项）

### 1. 基础配置

| 配置项 | 值 | 说明 |
| ------ | -- | ---- |
| `disclaimer_agreed` | ✅ | 同意免责声明（必填） |
| `enabled` | ✅ | 插件总开关 |
| `admin_list` | 你的QQ | 管理员名单（群管指令必填） |
| `auto_moderate_enabled` | ✅ | 自动审核总开关 |
| `scan_ad` | ✅ | 广告正则检测 |

### 2. 视觉识别（图片 OCR + 视频帧共用）

| 配置项 | 值 | 说明 |
| ------ | -- | ---- |
| `ocr_provider_id` | 智谱 Provider ID | 指定 GLM-4V-Flash 做视觉识别 |
| `ocr_enabled` | ✅ | 图片识图审核 |
| `llm_moderation_enabled` | ✅ | LLM 二次判断 |
| `qrcode_decode_enabled` | ✅ | 本地二维码解码（免费） |

### 3. 视频广告检测（v2.9.0+）

| 配置项 | 推荐 | 说明 |
| ------ | ---- | ---- |
| `video_audit_enabled` | ✅ | 视频广告检测总开关 |
| `video_max_frames` | `3` | 每视频最多抽帧数 |
| `video_max_size_mb` | `30` | 超过体积不检测 |
| `video_audit_timeout` | `60` | 单条消息审核总超时（秒） |

### 4. 感知哈希广告图黑名单（v2.9.1+，省视觉 API）

| 配置项 | 推荐 | 说明 |
| ------ | ---- | ---- |
| `ad_hash_blacklist_enabled` | ✅ | 广告样本黑名单 |
| `ad_hash_distance` | `10` | 相似度阈值（0-64） |
| `ad_hash_auto_learn` | ✅ | 广告确认后自动学习 |

### 5. 广告分级处置（v2.9.1+，降低误伤）

| 配置项 | 推荐 | 说明 |
| ------ | ---- | ---- |
| `ad_escalation_enabled` | ✅ | 分级处置总开关 |
| `ad_escalation_warn_at` | `1` | 第 1 次仅警告 |
| `ad_escalation_ban_at` | `2` | 第 2 次禁言 |
| `ad_escalation_kick_at` | `3` | 第 3 次踢出 |

### 6. 实时性优化（v2.9.2+，低配/省费）

| 配置项 | 推荐 | 说明 |
| ------ | ---- | ---- |
| `video_quick_precheck` | ✅ | 本地特征预检，低分帧跳过视觉 API |
| `video_fingerprint_cache` | ✅ | 广告视频整段指纹缓存，群发零调用 |
| `video_frame_mode` | `scene` | 场景切换抽帧（关键帧） |

> 以上配置均支持**按群覆盖**（WebUI「多群配置」）。

---

## 六、独立 Web 管理后台（v2.10.0+）

插件内置独立 HTTP 后台，浏览器直接访问，不依赖 AstrBot 面板：

| 配置项 | 值 | 说明 |
| ------ | -- | ---- |
| `ad_backend_enabled` | ✅ | 后台开关 |
| `ad_backend_port` | `8765` | 后台端口（防火墙放行） |
| `ad_backend_token` | 随机串 | 访问令牌（务必设置） |

保存后**重载插件**，日志出现 `独立管理后台已启动` 后访问：

```
http://服务器IP:8765/?token=你的令牌
```

后台提供：总览统计、违规记录（含图片证据）、广告黑名单管理、视频指纹管理、分级处置记录、配置状态。

---

### 公网服务器访问排障（连不上后台时按顺序排查）

公网服务器连不上独立后台，**90% 是端口没放行**。按顺序排查：

1. **云安全组放行端口**（最常被忽略）：
   - 阿里云：ECS 控制台 → 安全组 → 入方向规则 → 手动添加：协议 TCP、端口 `8765`、授权对象 `0.0.0.0/0`；
   - 腾讯云：安全组 → 入站规则 → 添加放行 TCP `8765`；
   - 华为云/其他云：类似，放行**入方向 TCP 8765**。

2. **服务器本地防火墙放行**：
   ```bash
   # firewalld（CentOS/OpenCloudOS 等）
   firewall-cmd --permanent --add-port=8765/tcp && firewall-cmd --reload
   # ufw（Ubuntu/Debian）
   ufw allow 8765/tcp
   ```

3. **确认后台真的启动了**：日志搜 `独立管理后台已启动`。若提示端口被占用，改 `ad_backend_port`（如 8766）并重载插件，再放行新端口。

4. **本机先自测**：服务器上执行 `curl.exe "http://127.0.0.1:8765/"`（Windows PowerShell 下务必用 `curl.exe` 并加引号，避免 `curl` 别名/`//` 解析问题）。返回 HTML → 后台正常，问题在网络放行；连接拒绝 → 后台没起来，看日志。

5. **带 Token 访问**：设置了 `ad_backend_token` 时，访问地址需带 `?token=你的令牌`。

### ⚠️ 一键安装（Docker）必须映射端口！

如果 AstrBot 是**一键安装（Docker）**部署，插件独立后台监听的是**容器内部**的 8765，
**必须**在 `compose.yml` 里映射到宿主机，否则安全组/防火墙放行也没用：

```bash
# 1. 找到并编辑 compose.yml
cd ~/AstrBot && nano compose.yml

# 2. 在 ports 段加一行：
#    ports:
#      - "6185:6185"
#      - "8765:8765"   ← 新增

# 3. 重建容器（必须 up -d，restart 不生效）
docker compose up -d

# 4. 验证
docker exec astrbot curl http://127.0.0.1:8765/
```

> 端口被宿主机占用时用 `"18000:8765"`（宿主机18000→容器8765），访问公网IP:18000 并放行 18000。

### 不想开放新端口？三个备选

- **用 AstrBot 面板内 WebUI**（推荐，无需额外端口）：AstrBot 面板（端口 6185，通常已能访问）自带的「群守护者」管理页，可看总览/违规记录/规则中心。
- **Nginx 反向代理**（复用 80/443）：把 `127.0.0.1:8765` 代理到域名路径，外部不用开 8765。
- **SSH 隧道**（仅本机临时用）：`ssh -L 8765:127.0.0.1:8765 用户@服务器IP -N`，然后本地访问 `http://127.0.0.1:8765`。

## 七、验证是否生效

1. **看日志**：AstrBot 日志搜索 `[GroupMgr]`；视频审核相关 `视频审核`、图片识别 `OCR`。
2. **实测**：
   - 发含广告文字的图片 → 应被撤回并提示；
   - 发含二维码/广告文字的视频 → 应被撤回并提示；
   - 正常图片/视频 → 放行（不误杀）。
3. **后台看数据**：打开独立后台「总览」页查看今日拦截统计。

---

## 八、常见问题

### Q1：日志提示「视频抽帧依赖 opencv-python-headless 不可用」

```bash
pip install opencv-python-headless numpy
```
重启 AstrBot。

### Q2：视觉识别无反应 / OCR LLM调用失败

1. 确认智谱 Provider 测试通过；
2. 确认 `ocr_provider_id` 填的是 Provider 的 **ID**；
3. 确认模型名 `glm-4v-flash`、服务器能访问 `open.bigmodel.cn`、实名认证已完成、免费额度充足。

### Q3：视频检测慢 / 想省钱

开启 `video_quick_precheck` + `video_fingerprint_cache`，再把 `video_frame_mode` 设为 `scene`。

### Q4：误杀正常内容

- 关闭对应功能或调严 `ad_hash_distance`（更小）；
- 开启 `ad_escalation_enabled` 分级处置，降低处罚强度；
- 在「多群配置」仅对可疑群开启。

### Q5：后台打不开

1. 确认插件日志显示后台已启动；
2. 防火墙放行 `ad_backend_port`；
3. 端口被占用时改 `ad_backend_port`。

### Q6：低配机器（2核2G）跑不动

- 关闭 `llm_moderation_always`（只对命中规则的消息送 LLM）；
- 降低 `video_max_frames` 到 2；
- 只开图片检测，视频检测按群覆盖单独开启。

---

## 附：数据文件位置

| 数据 | 位置 |
| ---- | ---- |
| 审核日志 | `data/plugin_data/astrbot_plugin_group_guardian/group_guardian.db` |
| 广告黑名单 | 同目录下 `hash_blacklist.json` |
| 视频指纹缓存 | 同目录下 `video_fingerprint_cache.json` |
| 分级处置记录 | 同目录下 `ad_escalation.json` |
