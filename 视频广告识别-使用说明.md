# 视频广告识别 · 使用说明

> 本功能为增强版新增能力（v2.9.0 起，`video_audit.py` + `hash_audit.py` + `ad_backend.py`），
> 用于自动识别群聊中的广告视频：**收集视频段 → SSRF 防护下载 → OpenCV 抽帧 → 逐帧视觉识别 + 二维码解码 → 并入统一审核流**。
> 所有开关默认关闭，纯增量，不影响原审核行为。

---

## 一、能识别什么

| 场景 | 识别手段 |
| --- | --- |
| 视频画面中的促销/引流/联系方式文字 | 视觉模型 OCR（与图片审核共用同一视觉模型） |
| 视频中嵌入的二维码/链接 | 本地二维码解码 |
| 与历史广告样本高度相似的画面 | 感知哈希（pHash）广告黑名单，命中直接标记「已知广告」 |
| 已确认过的同一广告视频（群发重复广告） | 视频指纹缓存（首帧哈希 + 时长指纹），秒识别、零视觉调用 |

## 二、工作原理

```
群消息出现 video 段（含回复引用）
   └─▶ 定位视频来源：组件 url / 本地路径 / 协议端 get_file 兜底
   └─▶ SSRF 防护下载（拒绝内网/本机地址，限体积/超时）
   └─▶ OpenCV 抽帧
         ├─ interval 模式：等间隔抽 N 帧（video_max_frames）
         └─ scene 模式：仅在画面明显变化时保留帧（省视觉调用）
   └─▶ 逐帧处理
         ├─ 可选快速预检（本地轻量特征过滤低可疑帧，省视觉调用）
         ├─ 视觉模型 OCR 识别画面文字
         └─ 二维码解码
   └─▶ 识别文本并入审核正文
   └─▶ 统一走「正则初筛 + LLM 二次判断 + 处罚」
   └─▶ 广告确认后：可选自动学习感知哈希入黑名单 / 指纹入缓存
```

识别出的文本与图片 OCR 一样进入统一审核管线，任一环节失败都静默降级，不误杀正常消息。

## 三、快速启用

**前置条件**：需要一个视觉模型 Provider（图片 OCR 与视频帧识别共用）。
推荐 [智谱 GLM-4V-Flash（免费）](https://open.bigmodel.cn/)，配置见 `使用教程-GLM4V-Flash.md`。

| 步骤 | 配置项 | 建议值 | 说明 |
| --- | --- | --- | --- |
| 1. 开启视频审核 | `video_audit_enabled` | `true` | **视频广告检测总开关**（默认关闭） |
| 2. 配置视觉模型 | `ocr_provider_id` | 智谱 Provider ID | 留空时回退 `moderation_llm_provider_id`，再留空用 AstrBot 默认 |
| 3. 按需开启省费用项 | `ad_hash_blacklist_enabled` | `true` | pHash 黑名单：历史广告画面直接命中，跳过视觉调用 |
| 4. 按需开启分级处置 | `ad_escalation_enabled` | `true` | 广告按窗口内次数升级：警告 → 禁言 → 踢出 |

> 所有配置均可在 WebUI「设置」或「多群配置」中按群覆盖。

## 四、成本控制选项

| 配置项 | 默认 | 作用 |
| --- | --- | --- |
| `video_max_frames` | `3` | 每个视频最多抽帧数（1-10），越大越准但视觉调用越多 |
| `video_frame_mode` | `interval` | `interval` 等间隔抽帧；`scene` 场景切换抽帧（关键画面，可显著减少无效帧）；`spans` 首/中/尾三段采样（v2.20.0，品牌露出/促销信息不遗漏） |
| `video_quick_precheck` | `false` | 开启后每帧先用本地轻量特征（饱和度/纹理/文字区域）预检，低可疑帧跳过视觉调用 |
| `video_fingerprint_cache` | `false` | 已确认广告整段视频（多帧哈希+时长桶，v2.20.0）下次直接命中，群发同一广告零视觉调用，容忍被裁剪/改时长 |
| `video_ad_visual_enabled` | `false` | v2.20.0 无文字广告判定：LLM 改用广告专用 prompt，品牌Logo/产品图/促销横幅等画面级广告也能识别 |
| `video_subtitle_boost` | `false` | v2.20.0 字幕带放大：画面下方字幕区裁剪放大 1.5 倍补识别，小字硬字幕不易漏 |
| `ad_hash_blacklist_enabled` | `false` | pHash 黑名单命中直接标记「已知广告」并跳过视觉调用（命中仍走 LLM 复核防误杀） |
| `video_max_bytes` | 见 schema | 视频体积上限（超过不下载） |
| `video_download_timeout` | `25` | 单个视频下载超时（秒） |
| `video_audit_timeout` | `60` | 单条消息视频审核总超时（秒），超过放弃本次审核 |

## 五、验证方法

1. 在 WebUI「设置」确认 `video_audit_enabled = true` 且 `ocr_provider_id` 已配置；
2. 群内发送一个含**二维码 / 广告文字**的短视频（如录屏的促销海报）；
3. 预期行为：
   - 视频被下载、抽帧、识别；
   - 识别出的广告文本命中规则或 LLM 判定违规 → **消息被撤回**（可选禁言）；
   - 审核日志（WebUI「违规记录」）出现对应记录，action 含「撤回」；
4. 再次发送**同一个**广告视频（开启指纹缓存后）：应命中 `video_fingerprint_cache`，秒级直接处置、零视觉调用；
5. 查看 `ad_backend`（独立后台，见下节）确认统计、黑名单、指纹、分级处置记录。

> 命中广告黑名单不会直接处罚，而是作为强信号进入统一审核流，由 LLM 复核兜底防误杀。

## 六、独立 Web 管理后台（ad_backend）

开启 `ad_backend_enabled = true` 后，插件内置独立 HTTP 后台（默认端口 `8765`，可配 `ad_backend_port`）：

- 今日/累计**广告拦截统计**；
- **违规记录**（含图片/视频帧证据）；
- **感知哈希广告黑名单**（查看/删除/自动学习记录）；
- **视频指纹缓存**（查看/清空）；
- **广告分级处置记录**（按群按用户）。

访问：`http://<服务器IP>:8765`；若配置了 `ad_backend_token`，需带 `?token=xxx` 或请求头 `X-Token`。
请勿将该端口直接暴露公网。

## 七、常见问题

| 问题 | 原因与处理 |
| --- | --- |
| 视频没有任何反应 | 检查 `video_audit_enabled` 是否开启、`ocr_provider_id` 是否配置有效；确认消息确为 video 段（非 file/图片） |
| 日志提示 `opencv-python-headless` 未安装 | `requirements.txt` 已声明依赖，检查插件安装时是否完整安装（`pip install opencv-python-headless`） |
| 视觉调用太多、费用高 | 开启 `video_quick_precheck`、`video_fingerprint_cache`、`ad_hash_blacklist_enabled`，或改用 `scene` 抽帧模式 |
| 大视频一直失败 | 提高 `video_download_timeout` / `video_audit_timeout` / `video_max_bytes`（有上限保护） |
| 担心误杀 | 全部默认关闭；`ad_hash_blacklist_enabled` 命中仍走 LLM 复核；广告分级处置（`ad_escalation_enabled`）可先警告后禁言 |

## 八、与 zcj-ui 原版的差异

| 能力 | 原版 v2.8.x | 本增强版 |
| --- | --- | --- |
| 视频广告检测（下载/抽帧/视觉识别/二维码解码） | ❌ 无 | ✅ v2.9.0 |
| 感知哈希广告黑名单 + 广告分级处置 | ❌ 无 | ✅ v2.9.1 |
| 场景抽帧 / 快速预检 / 指纹缓存 | ❌ 无 | ✅ v2.9.2 |
| 独立 Web 后台 + 本地 RapidOCR | ❌ 无 | ✅ v2.10.x |
| 多识别引擎（local/umi/cloud/llm/auto） | ❌ 无 | ✅ v2.11.x |
| 无文字广告判定 / 首中尾抽帧 / 字幕带放大 / 多帧鲁棒指纹 | ❌ 无 | ✅ v2.20.0 |

> 全部新功能**默认关闭**，升级后原有审核行为完全不变；按上文「快速启用」手动开启即可。

